import { KeyRound, ShieldCheck, UserRound } from 'lucide-react'
import Card from '../components/common/Card.jsx'
import Button from '../components/common/Button.jsx'
import { useAuth } from '../hooks/useAuth.js'
export default function Profile() { const { user } = useAuth(); const rows = [['Identity', user?.full_name ?? 'Marine Officer'], ['Role', user?.role ?? 'Marine Conservation Officer'], ['Email', user?.email ?? 'Not available'], ['Organization', user?.organization ?? 'Ocean Sentinel Agency']]; return <div className="space-y-6"><div><p className="eyebrow">Identity & access</p><h1 className="page-title mt-1">Profile</h1><p className="page-subtitle">Your verified operational identity and security settings.</p></div><div className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><Card className="flex flex-col items-center justify-center text-center"><div className="grid size-24 place-items-center rounded-[2rem] bg-gradient-to-br from-cyan-300 to-emerald-300 text-3xl font-black text-slate-950">{user?.full_name?.[0] ?? 'O'}</div><h2 className="mt-5 text-xl font-semibold text-white">{user?.full_name ?? 'Marine Officer'}</h2><p className="mt-1 text-sm text-cyan-200">Verified government access</p><div className="mt-6 flex items-center gap-2 text-xs text-emerald-200"><ShieldCheck size={15} />Identity verified</div></Card><Card title="Operational profile" eyebrow="Account record" action={<Button variant="secondary">Edit profile</Button>}><dl className="divide-y divide-cyan-100/8">{rows.map(([label, value]) => <div className="flex justify-between gap-4 py-4" key={label}><dt className="text-sm text-slate-500">{label}</dt><dd className="text-right text-sm font-medium text-white">{value}</dd></div>)}</dl><div className="mt-6 flex gap-3"><Button variant="secondary"><KeyRound size={16} />Change password</Button><Button variant="ghost"><UserRound size={16} />Session controls</Button></div></Card></div></div> }
