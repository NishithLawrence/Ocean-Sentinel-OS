import { Database, Shield, Users } from 'lucide-react'
import Card from '../components/common/Card.jsx'
import Badge from '../components/common/Badge.jsx'
export default function Admin() { const modules = [['Access control', 'Government roles and permissions', Shield, 'Protected'], ['Response teams', 'Operational capacity registry', Users, 'Synced'], ['Data sources', 'Reef and environmental datasets', Database, 'Healthy']]; return <div className="space-y-6"><div><p className="eyebrow">System governance</p><h1 className="page-title mt-1">Administration</h1><p className="page-subtitle">Platform health, access posture, and connected operational modules.</p></div><div className="grid gap-5 md:grid-cols-3">{modules.map(([title, description, Icon, state]) => <Card key={title}><div className="grid size-11 place-items-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Icon size={20} /></div><h2 className="mt-5 font-semibold text-white">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{description}</p><div className="mt-5"><Badge variant="success">{state}</Badge></div></Card>)}</div><Card title="System posture" eyebrow="Live status"><div className="grid gap-4 sm:grid-cols-3">{[['API gateway', 'Operational'], ['Data integrity', 'Verified'], ['Audit trail', 'Enabled']].map(([label, value]) => <div className="rounded-2xl border border-cyan-100/10 bg-white/3 p-4" key={label}><p className="text-sm text-slate-500">{label}</p><p className="mt-2 font-medium text-emerald-200">● {value}</p></div>)}</div></Card></div> }
