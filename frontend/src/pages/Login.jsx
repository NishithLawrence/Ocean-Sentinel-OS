import { Navigate } from 'react-router-dom'
import Button from '../components/common/Button.jsx'
import { useAuth } from '../hooks/useAuth.js'
export default function Login() { const { isAuthenticated } = useAuth(); if (isAuthenticated) return <Navigate to="/dashboard" replace />; return <main className="grid min-h-screen place-items-center p-6"><section className="w-full max-w-md rounded-lg bg-white p-8 shadow-sm"><h1 className="text-2xl font-bold text-ocean-700">Ocean Sentinel OS</h1><p className="mt-2 text-slate-600">Government officer sign-in</p><form className="mt-6 space-y-4"><label className="block text-sm font-medium">Email<input type="email" disabled className="mt-1 block w-full rounded-md border border-slate-300 p-2" /></label><label className="block text-sm font-medium">Password<input type="password" disabled className="mt-1 block w-full rounded-md border border-slate-300 p-2" /></label><Button disabled className="w-full opacity-60">Login (scaffold only)</Button></form></section></main> }
